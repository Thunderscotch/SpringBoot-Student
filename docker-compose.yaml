version: '3.8'
services:
  mongodb:
    image: mongo
    container_name: mongo-db
    ports:
      - "27017:27017"
  bootstrapping:
    image: demo
    build: .
    ports:
      - "8086:8086"
    depends_on:
      - mongodb
